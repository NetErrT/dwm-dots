#!/usr/bin/env sh

ascii_distro="Void_small"

print_info() {
	info title
	info underline

	info "OS" distro
	info "CPU" cpu
	info "GPU" gpu
	info "Kernel" kernel
	info "Packages" packages
	info "Shell" shell
	info "WM" wm
	info "Theme" theme
	info "Icons" icons
	info "Terminal" term

	info cols
}

